# Dependencies are restored from package.json and pnpm-lock.yaml.
node_modules/
.pnpm-store/

# Next.js generated output and local tooling files.
.next/
out/
*.tsbuildinfo

# Local environment files (never commit secrets).
.env*
!.env.example

# OS/editor noise.
.DS_Store
Thumbs.db
